{
  "name": "mypoolr-telegram-bot",
  "version": "1.0.0",
  "description": "MyPoolr Telegram Bot on Cloudflare Workers",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production"
  },
  "dependencies": {
    "node-telegram-bot-api": "^0.64.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}